import { IfNotRoleDirective } from './if-not-role.directive';

describe('IfNotRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new IfNotRoleDirective();
    expect(directive).toBeTruthy();
  });
});
